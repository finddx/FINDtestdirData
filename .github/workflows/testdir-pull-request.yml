on:
  push:
    branches: [preview]

name: update-testdir

jobs:
  update-testdir:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v3
        with:
          ref: main
